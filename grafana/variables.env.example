# Copy to variables.env (gitignored) and adjust when bringing the lab online.
# No secrets belong here — Grafana authenticates to Google Cloud with the
# runtime service account / Application Default Credentials.
GCP_PROJECT=policy-as-code-platform
GRAFANA_DASHBOARD_PATH=./grafana/dashboards
